// const {Router} = require("express");
// const multer = require('multer');
// const {getPlants, savePlant } = require("../controllers/PlantController");

// const storage = multer.memoryStorage();
// const upload = multer({ storage });

// const router = Router();

// router.get("/plant/getAll", getPlants);
// router.post("/plant/save", upload.single('image'), savePlant);

// module.exports = router